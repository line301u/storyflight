---
import BaseLayout from '../layouts/BaseLayout.astro';
import Arrow from '../assets/icons/CircleWithArrow.svelte';
---


<BaseLayout>

<main class="success-page">

<div class="success-wrapper">
<h1 class="caption">Tak!</h1>
<p class="container container--fluid">Vi har modtaget din henvendelse og du vil høre fra os snarest.</p>

<a class="links-hover container container--fluid" href="/" ><span class="arrow_icon"><Arrow /></span>Tag mig til forsiden </a
>

</div>

</main>


</BaseLayout>

<style lang="scss">

.success-page{
    height: 100vh;
    background: var(--clr-secondary);
    display: flex;
    justify-content: center;
    align-items: center;


    .success-wrapper{
        width: 400px;
        height: 300px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;


        @media (min-width: 700px){
            width: 700px;
        }
        .caption{
          color: var(--clr-white);
          margin-bottom: 1rem;
        }

        p{
            text-align: center;
            color: var(--clr-white)
        }
    }

    a {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    color: black;
    padding-top: 2.8rem;
    color: var(--clr-white);
    .arrow_icon {
      color: var(--clr-primary);
      height: 28px;
      width: 28px;
      margin-right: 1em;
      padding-top: 0px;
      transform: rotate(180deg)
    }
  }
    
}

</style>